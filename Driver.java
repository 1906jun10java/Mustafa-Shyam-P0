package project0;

import java.util.HashMap;

public class Driver {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*HashMap<String, String> map=  new HashMap<>();
		
		boolean authenticate(String username, int noOfAttempts) {
			Credentials ceredentials = getCredentials(username);
			
			for(int attempt = 1; attempt < noOfAttempts; attempt++) {
				char[] password = promptForPassword();
				
				if(credentials.match(passowrd)) {
					return true;
				}else {
					wrongPassword(attempt, noOfAttempts);
					
				}
			}
			return false;
		}*/
	
		
		
		}
		
		

	}



